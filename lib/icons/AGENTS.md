# Built-In Icon Agents

Package scope: `@dpeek/formless-icons`.

Own stable built-in icon identities and trusted renderer-neutral SVG sources. Keep React, App schema, runtime, picker metadata, and source parsing out of production code.
